[services.doctor]
address = "doctor-service:8081"
timeout = 15

[server]
port = 8082

[database]
type = "sqlite3" # supported: sqlite3

[database.sqlite3]
source = "./db/appointment.db"

[message-broker]
type = "nats" # supported: nats

[message-broker.nats]
connection_url = "message-broker-nats"